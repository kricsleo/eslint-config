{
  "name": "@kricsleo/eslint-config",
  "version": "0.0.0",
  "description": "Kricsleo's ESlint config",
  "author": "Kricsleo <kricsleo@163.com>",
  "license": "MIT",
  "keywords": [
    "ESlint"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "bumpp && npm publish",
    "lint": "eslint playground/**/* -c index.js --no-cache",
    "lint:fix": "eslint playground/**/* -c index.js --fix --no-cache"
  },
  "dependencies": {
    "@antfu/eslint-config": "^0.38.5"
  },
  "devDependencies": {
    "bumpp": "^9.1.0",
    "eslint": "^8.38.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
